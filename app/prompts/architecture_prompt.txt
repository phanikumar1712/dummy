Review this pull request diff for architecture and design issues.

Check for:
- Tight coupling between layers
- Business logic in the wrong layer
- Poor modularity or missing abstractions
- Scalability concerns
- Violations of separation of concerns
- Circular dependencies

Return a STRICT JSON array only. Each item must have:
file, severity (LOW|MEDIUM|HIGH|CRITICAL), category (ARCHITECTURE), issue, suggestion

If no issues are found, return [].

Diff:
{diff}
